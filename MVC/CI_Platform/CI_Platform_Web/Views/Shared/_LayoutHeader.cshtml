@using CI_Platform_Web.Entities.Models;

@{
    var usrid = Context.Session.GetString("userID");
}

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - CI_Platform_Web</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.3/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    @*<link rel="stylesheet" href="~/css/home.css" />*@
    <link rel="stylesheet" href="~/CI_Platform_Web.styles.css" asp-append-version="true" />
</head>
<body>


    <!-- Starting Top Header -->
    <div class="abc">
        <div class="container">
            <div class="row">

                <div class="col-6 d-flex align-items-center headerLeft">

                    <!-- Top-Header Shown In Mobile/Tab -->

                    <ul class="menu">

                        <a href="./stories.html">Stories</a>

                        <div class="dropdown policy-1">
                            <button class="btn btn-secondary dropdown-toggle btnCustom" type="button" data-bs-toggle="dropdown"
                                    aria-expanded="false">
                                Policy
                            </button>
                            <ul class="dropdown-menu">
                                <li><a class="dropdown-item" href="./privacy.html">Privacy Policy</a></li>
                                <li><a class="dropdown-item" href="#">Something else here</a></li>
                            </ul>
                        </div>

                        <!--Country Dropdown-->
                        <div class="dropdown col-3">

                            <button class="btn btn-secondary dropdown-toggle btnCustom" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                                Country
                            </button>
                            <ul class="dropdown-menu" style="height:250px; overflow-y:scroll;">

                                <!-- for country data in drop down -->
                                @if (ViewBag.Country != null)
                                {
                                    @foreach (var item in ViewBag.Country)
                                    {
                                        <li>
                                            <a class="dropdown-item p-0 mt-1" href="#">
                                                <input type="checkbox" id="" class="checkbox ms-3 " value="@item.Name">
                                                @item.Name
                                            </a>
                                        </li>
                                    }
                                }

                            </ul>

                            @*                        <select id="Country11">
                                <option>default</option>
                                </select>

                                <select id="City11">
                                <option>default</option>
                                </select>*@
                        </div>

                        <!--City Dropdown-->
                        <div class="dropdown col-3">
                            <button class="btn btn-secondary dropdown-toggle btnCustom" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                                City
                            </button>
                            <ul class="dropdown-menu" style="height:250px; overflow-y:scroll;">

                                <!-- for city data in drop down -->
                                @if (ViewBag.City != null)
                                {
                                    @foreach (var item in ViewBag.City)
                                    {
                                        <li>
                                            <a class="dropdown-item p-0 mt-1" href="#">
                                                <input type="checkbox" id="" class="checkbox ms-3 " value="@item.Name">
                                                @item.Name
                                            </a>
                                        </li>
                                    }
                                }

                            </ul>
                        </div>

                        <!--Theme Dropdown-->
                        <div class="dropdown col-3  ">
                            <button class="btn btn-secondary dropdown-toggle btnCustom" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                                Themes
                            </button>
                            <ul class="dropdown-menu" style="height:250px; overflow-y:scroll;">

                                <!--for theme data in drop down-->
                                @if (ViewBag.MissionThemes != null)
                                {
                                    @foreach (var item in ViewBag.MissionThemes)
                                    {
                                        <li>
                                            <a class="dropdown-item p-0 mt-1" href="#">
                                                <input type="checkbox" id="" class="checkbox ms-3 " value="@item.Title">
                                                @item.Title
                                            </a>
                                        </li>
                                    }
                                }

                            </ul>
                        </div>

                        <!--Skill Dropdown-->
                        <div class="dropdown col-3">
                            <button class="btn btn-secondary dropdown-toggle btnCustom" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                                Skills
                            </button>
                            <ul class="dropdown-menu" style="height:250px; overflow-y:scroll;">

                                <!-- for skill data in drop down -->
                                @if (ViewBag.Skill != null)
                                {
                                    @foreach (var item in ViewBag.Skill)
                                    {
                                        <li>
                                            <a class="dropdown-item p-0 mt-1" href="#">
                                                <input type="checkbox" id="" class="checkbox ms-3 " value="@item.skill_name">
                                                @item.skill_name
                                            </a>
                                        </li>
                                    }
                                }

                            </ul>
                        </div>

                    </ul>

                    <button class="hamburger">
                        <!-- material icons https://material.io/resources/icons/ -->
                        <i class="menuIcon material-icons">menu</i>
                        <i class="closeIcon material-icons">close</i>
                    </button>


                    <!-- Top-Header Shown On PC/Laptop -->


                    <a href="./stories.html" class="topHeader">Stories</a>

                    <div class="dropdown topHeader">
                        <button class="btn btn-secondary dropdown-toggle btnCustom" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Policy
                        </button>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="./privacy.html">Privacy Policy</a></li>
                            <li><a class="dropdown-item" href="#">Something else here</a></li>
                        </ul>
                    </div>

                </div>

                <!-- Common In Both -->

                <div class="col-6 py-2">
                    <div class="d-flex align-items-center justify-content-end">

                        <div><img src="~/images/user-img.png" alt="" class="imgIcon"></div>

                        <div class="dropdown ">
                            <button class="btn btn-secondary dropdown-toggle btnCustom" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                                @{
                                    if (@usrid != null)
                                    {
                                        <span class="txt text-dark">@usrid</span>
                                    }
                                }
                            </button>
                            <ul class="dropdown-menu">
                                <li><a class="dropdown-item" href="#">My Profile</a></li>
                                <li><a class="dropdown-item" href="#">Volunteering Timesheet</a></li>
                                <li><a class="dropdown-item" asp-controller="Login" asp-action="Login">Logout</a></li>
                            </ul>
                        </div>

                        @*
                            <ul class="d-flex text-decoration-none" style="list-style:none">
                            <li><a class="dropdown-item btn" asp-controller="Home" asp-action="Index">Login</a></li>
                            <li><a class="dropdown-item btn" asp-controller="Home" asp-action="Registration">Register</a></li>
                            </ul>
                        *@

                    </div>
                </div>

            </div>
        </div>
        <hr class="line mt-1 mb-0">

        <!-- Searching And Filter Header In PC/Tab -->

        <div class="container">
            <div class="row">

                <div class="col-6 d-flex align-items-center">

                    <img src="~/images/search.png" alt="Search" class="searchIcon">

                    <nav class="navbar">
                        <div class="container">
                            <form class="d-flex" role="search">
                                <input class="form-control me-2" id="search-bar" name="searchQuery" value="@ViewBag.searchQuery" type="text" placeholder="Search mission..." aria-label="Search">
                            </form>
                        </div>
                    </nav>

                </div>

                <div class="col-6 d-flex align-items-center">

                    <!--Country Dropdown-->
                    @*                    <div class="dropdown col-3 filterNav">

                        <button class="btn btn-secondary dropdown-toggle btnCustom" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Country
                        </button>
                        <ul class="dropdown-menu" style="height:250px; overflow-y:scroll;">

                        <!-- for country data in drop down -->
                        @if (ViewBag.Country != null)
                        {
                        @foreach (var item in ViewBag.Country)
                        {
                        <li>
                        <a class="dropdown-item p-0 mt-1" href="#">
                        <input type="checkbox" id="" class="checkbox ms-3 " value="@item.Name">
                        @item.Name
                        </a>
                        </li>
                        }
                        }

                        </ul>
                        </div>*@


                    <div class="dropdown col-3 filterNav">
                        <ul class="navbar-nav mr-auto">
                            <li class="nav-accordion-item dropdown">
                                <select class="form-select" id="Country" style="padding:1rem;">
                                    <option selected>Country</option>
                                </select>
                            </li>
                        </ul>
                    </div>

                    <!--City Dropdown-->
                    @*                    <div class="dropdown col-3 filterNav">
                        <button class="btn btn-secondary dropdown-toggle btnCustom" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                        City
                        </button>
                        <ul class="dropdown-menu" style="height:250px; overflow-y:scroll;">

                        <!-- for city data in drop down -->
                        @if (ViewBag.City != null)
                        {
                        @foreach (var item in ViewBag.City)
                        {
                        <li>
                        <a class="dropdown-item p-0 mt-1" href="#">
                        <input type="checkbox" id="" class="checkbox ms-3 " value="@item.Name">
                        @item.Name
                        </a>
                        </li>
                        }
                        }

                        </ul>
                        </div>*@

                    <div class="dropdown col-3 filterNav">
                        <button class="btn btn-secondary dropdown-toggle btnCustom" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                            City
                        </button>
                        <ul class="dropdown-menu" style="height:250px; overflow-y:scroll;">

                            <!-- for city data in drop down -->
                            <li>
                                <a class="dropdown-item p-0 mt-1" href="#" id="City">

                                </a>
                            </li>

                        </ul>
                    </div>

                    <!--Theme Dropdown-->
                    <div class="dropdown col-3 filterNav">
                        <button class="btn btn-secondary dropdown-toggle btnCustom" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Themes
                        </button>
                        <ul class="dropdown-menu" style="height:250px; overflow-y:scroll;">

                            <!--for theme data in drop down-->
                            @if (ViewBag.MissionThemes != null)
                            {
                                @foreach (var item in ViewBag.MissionThemes)
                                {
                                    <li>
                                        <a class="dropdown-item p-0 mt-1" href="#">
                                            <input type="checkbox" id="" class="checkbox ms-3 " value="@item.Title">
                                            @item.Title
                                        </a>
                                    </li>
                                }
                            }

                        </ul>
                    </div>

                    <!--Skill Dropdown-->
                    <div class="dropdown col-3 filterNav">
                        <button class="btn btn-secondary dropdown-toggle btnCustom" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Skills
                        </button>
                        <ul class="dropdown-menu" style="height:250px; overflow-y:scroll;">

                            <!-- for skill data in drop down -->
                            @if (ViewBag.Skill != null)
                            {
                                @foreach (var item in ViewBag.Skill)
                                {
                                    <li>
                                        <a class="dropdown-item p-0 mt-1" href="#">
                                            <input type="checkbox" id="" class="checkbox ms-3 " value="@item.skill_name">
                                            @item.skill_name
                                        </a>
                                    </li>
                                }
                            }

                        </ul>
                    </div>

                </div>



            </div>

        </div>



        @RenderBody()

    </div>


    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/filter.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    <script>


        //For the Hamburger navbar

          const menu = document.querySelector(".menu");
          const menuItems = document.querySelectorAll(".menuItem");
          const hamburger = document.querySelector(".hamburger");
          const closeIcon = document.querySelector(".closeIcon");
          const menuIcon = document.querySelector(".menuIcon");

          function toggleMenu() {
              if (menu.classList.contains("showMenu")) {
                  menu.classList.remove("showMenu");
                  closeIcon.style.display = "none";
                  menuIcon.style.display = "block";
              } else {
                  menu.classList.add("showMenu");
                  closeIcon.style.display = "block";
                  menuIcon.style.display = "none";
              }
          }

          hamburger.addEventListener("click", toggleMenu);







        // For the Grid View And List View

        function showList(e) {
            localStorage.setItem("view","list");
            var $gridCont = $('.grid-container');
            e.preventDefault();
            $gridCont.hasClass('list-view') ? $gridCont.removeClass('list-view') : $gridCont.addClass('list-view');
        }
        function gridList(e) {
            localStorage.setItem("view","grid");
            var $gridCont = $('.grid-container')
            e.preventDefault();
            $gridCont.removeClass('list-view');
        }

        $(document).on('click', '.btn-grid', gridList);
        $(document).on('click', '.btn-list', showList);


    </script>
    @await RenderSectionAsync("Scripts", required: false)


</body>

</html>